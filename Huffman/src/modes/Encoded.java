package modes;

import tree.Tree;

import java.util.HashMap;

public class Encoded {
    private final Tree tree;
    private final HashMap<Character, String> codes = new HashMap<>();

    public void generateCodes(String content) {
        
    }
}
